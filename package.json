{
  "name": "tslint-presets",
  "version": "1.0.3",
  "description": "A set of tslint rules",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Boulangerie/tslint-presets"
  },
  "main": "./tslint.json",
  "scripts": {
    "bump": "npm version patch && git push --tags origin master",
    "bump:minor": "npm version minor && git push --tags origin master",
    "bump:major": "npm version major && git push --tags origin master"
  },
  "author": "alexandre.moatty@gmail.com",
  "license": "MIT",
  "dependencies": {
    "tslint": "^5.5.0",
    "tslint-eslint-rules": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^2.3.4"
  }
}
